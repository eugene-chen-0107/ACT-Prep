# Feature modules

Planned domains: `auth`, `diagnostics`, `practice`, `questions`, `mistakes`, `progress`, `study-plans`, and `settings`. Each feature can own its UI, schemas, and domain services; shared persistence and external-provider adapters belong in `src/db` and `src/lib`.
